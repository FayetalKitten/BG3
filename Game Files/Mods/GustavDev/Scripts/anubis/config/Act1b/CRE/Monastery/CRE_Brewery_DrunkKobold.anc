game.configs.CRE_Brewery_DrunkKobold = Config{
    params={
        DrunkDance = {
            type = EParamType.String,
            help = [[The dancing animation for this Kobold.]],
            required = false,
        },
    },
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.CRE_Brewery_DrunkKobold,
            DrunkDance = params.DrunkDance,
        },
    }
}
