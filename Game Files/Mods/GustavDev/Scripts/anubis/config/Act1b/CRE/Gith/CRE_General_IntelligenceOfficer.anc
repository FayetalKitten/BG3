game.configs.CRE_General_IntelligenceOfficer = Config{
    params={
        DefaultPos = {
            type = EParamType.String,
            help = [[Default position for the character to be in]],
            required = true,
        },
    },
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.CRE_General_IntelligenceOffier,
            DefaultPos = params.DefaultPos,
        },
    }
}
