game.configs.LOW_CazadorsPalace_Ballroom_Wolf = Config {
    params = {
        atCorpsePos = {
            type = EParamType.String,
            required = true,
        },
    },

    root = StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.LOW_CazadorsPalace_Ballroom_Wolf,
            atCorpsePos = params.atCorpsePos,
        },
    }
}