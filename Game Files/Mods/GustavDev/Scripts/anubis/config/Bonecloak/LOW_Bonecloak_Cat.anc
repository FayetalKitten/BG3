game.configs.LOW_Bonecloak_Cat = Config{
    params={
        streetWanderTrigger = {
            type = EParamType.String,
            help = [[Default wander trigger for the cat]],
            required = false,
        },
    },
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.LOW_Bonecloak_Cat,
            streetWanderTrigger = params.streetWanderTrigger,
        },
    }
}
