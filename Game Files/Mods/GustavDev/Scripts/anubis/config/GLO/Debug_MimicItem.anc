game.configs.Debug_MimicItem = Config{
    params = {
        Mimic = {type = EParamType.String, required=true, help=[[
            Character entity representing the mimic itself
        ]]}
    },

    root=StateRef{game.roots.DefaultItem,
        idle = StateRef{game.states.Debug_MimicItem,
            Mimic = params.Mimic
        }
    }
}
